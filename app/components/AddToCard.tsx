"use client";
import React from "react";

const AddToCard = () => {
  return (
    <div>
      <button 
        onClick={() => {
          "just clicked";
        }}
      >
        Add to card
      </button>
    </div>
  );
};

export default AddToCard;
